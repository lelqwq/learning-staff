#include <string.h>

static void Eliminate_extra_spaces(char str[])//消除字符串中多余空格
{
	int i = 0, j = 0;
	int n = 0;//当前检测到的空格数量
	for (; i < strlen(str); ++i)
	{
		n = (str[i] == ' ') ? n + 1 : 0;
		str[j] = str[i];
		if (n <= 1) j++;
	}
	str[j] = '\0';
}